import type { LucideIcon } from "lucide-react";

export interface SOCIAL_LINKS_TYPES {
    label: string;
    href: string;
    icon: LucideIcon;
}

export interface FACILITIES_TYPES {
    id: string;
    label: string;
}
